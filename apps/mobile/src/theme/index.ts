import {
  nativeColors,
  nativeControls,
  nativeRadii,
  nativeSpacing,
} from "@salimon/ui-tokens"

export const mobileTheme = {
  colors: nativeColors,
  controls: nativeControls,
  radii: nativeRadii,
  spacing: nativeSpacing,
} as const
